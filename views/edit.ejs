<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>
<body>
    <div class="w-full min-h-screen bg-[#2C2638] text-white p-10">

        <h5 class="mb-5 text-2xl font-semibold"> Edit post</h5>
        <form action="/update/<%= post._id%>" method="post">
            <textarea name="editedContent" class="bg-[#3C364C] resize-none outline-none p-3 rounded-xl h-40   border-2 border-zinc-800 w-1/3" placeholder="what's on your mind"><%=post.content%></textarea>
            <input type="submit"  value="Update Post" class=" w-40 mt-5 rounded-md text-sm px-3 py-2 block bg-yellow-500">
        </form>

    </div>
</body>
</html>